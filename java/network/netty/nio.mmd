Mind Map generated by NB MindMap plugin
> __version__=`1.1`,showJumps=`true`
---

# Netty

## epoll

### trigger type

#### level triggered

#### edge\-triggered

### epoll\_create

#### create an epoll instance so later io fds can be registered to it

### epoll\_ctl

#### put the sockets fds and the event we interested to epoll instance

### epoll\_wait

#### waiting for interested events happend, this method will be blocked if no events happen

### adjust maximum fd epoll can use<br/>/proc/sys/fs/epoll/max\_user\_watches<br/>fd cost: 160 bytes on 64\-bit kernel

## kqueue

## select

## poll

## thread incurred overhead
> leftSide=`true`


### JVM default per thread costs 1MB

### context switch
> leftSide=`true`

